extends layout

block content
  h1= title
  p Welcome to #{title}

  p Mounitha Vemula
  
  button(type="button",onclick="doReduce()" class="btn btn-info") Press Me

  div(id="reduce") Reduce the value
  script.
    var count=20;
    var change=-1;
    function doReduce() {
      count = count+change;
      change--;
      document.getElementById("reduce").innerHTML=`Count ${count} with change ${change}`
    }
